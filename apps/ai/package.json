{
  "name": "@growlogue/ai-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "build": "wrangler deploy --dry-run",
    "lint": "eslint src",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "cf:typegen": "wrangler types --env-interface CloudflareEnv worker-configuration.d.ts",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "@growlogue/ai-coach": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "24.10.1",
    "eslint": "9.39.2",
    "typescript": "5.9.3",
    "vitest": "4.1.10",
    "wrangler": "4.114.0"
  }
}
